# Archives

这里存放历史规格、计划和旧目录迁移结果。归档内容只用于追溯背景，不作为当前实现事实源。

| 路径 | 用途 |
| --- | --- |
| `specs/` | 已完成或废弃的历史规格。 |
| `plans/` | 已完成、废弃或被替代的历史实施计划、工程审计和执行跟踪记录。 |
| `plans/legacy-plan/` | 从旧 `docs/plan` 目录整体迁入的历史资料。 |

需要恢复归档内容时，先把相关结论沉淀到 `docs/superpowers/specs/` 或 `docs/superpowers/plans/`，不要直接从归档执行。
