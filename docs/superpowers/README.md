# Superpowers Workspace

这里存放当前或近期需求的规格与执行计划。它是工作区，不是长期事实源。

| 路径 | 用途 |
| --- | --- |
| `specs/` | 需求规格、设计边界、验收标准。 |
| `plans/` | 针对规格的实施计划、审计记录、执行清单。 |

完成或废弃后，把对应文件移到 `../archives/specs/` 或 `../archives/plans/`。

