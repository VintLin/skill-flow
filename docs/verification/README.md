# Verification

这里存放验证矩阵、测试覆盖说明和 UI 状态矩阵。它们定义应验证的行为，不替代 spec 或 plan。

| 文件 | 用途 |
| --- | --- |
| `GROUP_CARD_STATE_MATRIX.md` | macOS 桌面端 `SharedGroupCard` 页面状态矩阵。 |
| `mount-lifecycle-matrix.md` | mount / unmount 生命周期边界场景矩阵。 |

