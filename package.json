{
  "name": "skill-flow-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @skill-flow/core build && npm run -w @skill-flow/tui build && npm run -w skill-flow build",
    "test": "npm run -w @skill-flow/core test && npm run -w @skill-flow/tui test && npm run -w skill-flow test",
    "test:watch": "npm run -w skill-flow test:watch"
  }
}
