# Release vNEXT

## Unreleased

- macOS packages without an Apple Developer certificate now use a stable per-architecture, cross-version ad-hoc identity, sealed bundle resources, and clean archive metadata so same-architecture updates can replace an already approved app without repeated quarantine-removal commands.
